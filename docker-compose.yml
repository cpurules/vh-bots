version: '3.8'

services:
  vh-db:
    build:
      context: ./vh-bots-db/
    image: vh-bots/db
    ports:
      - 8529:8529
    volumes:
      - vh-db:/var/lib/arangodb3
    environment:
      - ARANGO_RANDOM_ROOT_PASSWORD=1
    container_name: 'vh_db'

volumes:
  vh-db:
